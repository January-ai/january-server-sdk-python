[project]
name = "january-server-fastapi-example"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115,<1",
  "januaryai-server",
  "python-dotenv>=1.0",
  "uvicorn>=0.34,<1",
]

[tool.uv.sources]
januaryai-server = { path = "../..", editable = true }
