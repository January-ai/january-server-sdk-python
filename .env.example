# Copy to .env only if it does not exist, then set your full sk- server API key.
# Never commit or share .env. Keep deployed keys in your platform's secret manager.
# Quickstarts and opt-in live runs contact production and consume credits.
JANUARY_API_KEY=
