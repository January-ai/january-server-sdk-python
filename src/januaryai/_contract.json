{
  "schemas": {
    "ErrorResponse": {
      "type": "object",
      "properties": {
        "message": {
          "type": "string",
          "description": "A developer-facing explanation of what went wrong and how to fix it.",
          "example": "query is required: the food name to search for, e.g. ?query=greek yogurt."
        },
        "code": {
          "type": "string",
          "description": "A stable machine-readable identifier for the class of failure — build retry logic on this, never on message wording.\n\nAny request, each with the status it usually accompanies: `invalid_request` (400), `unauthorized` (401), `forbidden` (403), `not_found` (404), `payload_too_large` (413), `rate_limited` (429), `credit_limit_exceeded` (429), `internal_error` (500), `not_implemented` (501), `upstream_error` (502), `service_unavailable` (503), `upstream_timeout` (504). Those pairings are the common case, not a guarantee: a status we do not map falls back to `invalid_request` below 500 and `internal_error` at or above it, so an internal service answering 409 or 422 reaches you with that status and `code: invalid_request`. Branch on the code first and treat the status as the fallback, exactly as for a code you do not recognise.\n\nClient tokens add six an API key never produces: `token_expired`, `token_invalid`, `token_revoked` (401), and `client_token_not_allowed`, `scope_insufficient`, `end_user_id_mismatch` (403). Each response documents its own.\n\nThree more are specific to individual endpoints: `end_user_id_required` (400 — an sk- key called a food-log operation with no January-End-User-ID header), `date_range_too_large` (400 — a food-log date range past the documented maximum), and `client_token_revocation_incomplete` (503 — a revocation call that only stopped part of its batch; the same request is safe to repeat).\n\n`POST /v1.2/food-analysis/image` adds four 400s about the image itself: `image_unreachable` (the URL could not be fetched), `image_corrupt` (the file could not be decoded), `image_format_unsupported` and `image_invalid_base64`. Each is fixed by the caller; the same image fails the same way again.\n\nRetry only `rate_limited`, `internal_error`, `upstream_error`, `service_unavailable`, `upstream_timeout` and `client_token_revocation_incomplete`, with backoff — `not_implemented` is permanent until the feature ships, so its 5xx status is not a reason to retry it. Two more the status code alone gets wrong: `credit_limit_exceeded` is a 429 that **must never be retried** — the allowance returns next calendar month, so a client that backs off on every 429 will spin until then; and `token_expired` is refreshed, not retried — mint a new token, then retry once.\n\nNew codes may be added over time; treat an unknown code according to its HTTP status class.",
          "example": "invalid_request"
        }
      },
      "required": [
        "message",
        "code"
      ],
      "x-january-upstream-schema": "ApiError"
    },
    "CreateClientTokenBody": {
      "type": "object",
      "properties": {
        "end_user_id": {
          "type": "string",
          "description": "Your stable ID for the end user this token acts as. The token is bound to it; requests made with the token act only on this user.",
          "example": "acme-user-8271",
          "maxLength": 64
        },
        "scopes": {
          "type": "array",
          "description": "What the token may do. **Required** — name only the scopes this token needs (least privilege), never the full set out of convenience. A read-only food-lookup screen asks for `[\"foods:read\"]`; a logging screen adds `food_logs:write`. Valid scopes: foods:read, food_analysis:write, food_logs:read, food_logs:write, glucose:read, restaurants:read.",
          "example": [
            "foods:read"
          ],
          "items": {
            "type": "string",
            "enum": [
              "foods:read",
              "food_analysis:write",
              "food_logs:read",
              "food_logs:write",
              "glucose:read",
              "restaurants:read"
            ]
          }
        },
        "ttl_seconds": {
          "type": "integer",
          "description": "How long the token stays valid, in seconds. Between 300 and 7200; defaults to 1800.",
          "minimum": 300,
          "maximum": 7200,
          "default": 1800,
          "example": 1800
        }
      },
      "required": [
        "end_user_id",
        "scopes"
      ],
      "x-january-upstream-schema": "CreateClientTokenRequest"
    },
    "ClientToken": {
      "type": "object",
      "properties": {
        "token": {
          "type": "string",
          "description": "The credential itself. Shown exactly once — it is stored only as a hash and can never be retrieved again.",
          "example": "ct-4fQr7yNb2KcXm9TvLpZ3wHs6JdRg8AeYuQ1oViB5xCn"
        },
        "expires_in": {
          "type": "integer",
          "description": "Seconds until the token expires, counted from the moment this response was produced. Compute expiry from this rather than from `expires_at` — a device clock that is wrong makes an absolute timestamp wrong with it.",
          "example": 1800
        },
        "expires_at": {
          "type": "string",
          "format": "date-time",
          "description": "The same expiry as an absolute UTC instant, for logs and humans.",
          "example": "2026-08-26T18:35:11.000Z"
        },
        "end_user_id": {
          "type": "string",
          "description": "The end user this token is bound to, echoed back so a caller can assert it minted what it meant to.",
          "example": "acme-user-8271"
        },
        "scopes": {
          "type": "array",
          "description": "What this token may do — the exact scopes it was granted, echoed back so a caller can assert it minted what it meant to.",
          "example": [
            "foods:read",
            "food_logs:read"
          ],
          "items": {
            "type": "string",
            "enum": [
              "foods:read",
              "food_analysis:write",
              "food_logs:read",
              "food_logs:write",
              "glucose:read",
              "restaurants:read"
            ]
          }
        }
      },
      "required": [
        "token",
        "expires_in",
        "expires_at",
        "end_user_id",
        "scopes"
      ],
      "x-january-upstream-schema": "ClientToken"
    },
    "RevokeClientTokensBody": {
      "type": "object",
      "properties": {
        "end_user_id": {
          "type": "string",
          "description": "The end user whose outstanding client tokens should all be revoked.",
          "example": "acme-user-8271",
          "maxLength": 64
        }
      },
      "required": [
        "end_user_id"
      ],
      "x-january-upstream-schema": "RevokeClientTokensRequest"
    },
    "ClientTokenRevocationResult": {
      "type": "object",
      "properties": {
        "revoked_count": {
          "type": "integer",
          "description": "How many live tokens this call revoked. Already-revoked and already-expired tokens are not counted, so an immediate second call reports 0 — repeat the call until it does.",
          "example": 2
        }
      },
      "required": [
        "revoked_count"
      ],
      "x-january-upstream-schema": "ClientTokenRevocationResult"
    },
    "CreditBalance": {
      "type": "object",
      "properties": {
        "plan": {
          "type": "string",
          "description": "The plan this allowance comes from.",
          "example": "free"
        },
        "period_start": {
          "type": "string",
          "format": "date",
          "description": "First day of the current billing period (UTC), inclusive.",
          "example": "2026-08-01"
        },
        "period_end": {
          "type": "string",
          "format": "date",
          "description": "Last day of the current billing period (UTC), inclusive.",
          "example": "2026-08-31"
        },
        "resets_at": {
          "type": "string",
          "format": "date-time",
          "description": "When the allowance resets and `used_credits` returns to 0.",
          "example": "2026-09-01T00:00:00.000Z"
        },
        "included_credits": {
          "type": "integer",
          "nullable": true,
          "description": "Credits included in the plan for this period, or `null` when the plan has no ceiling.",
          "example": 1000
        },
        "used_credits": {
          "type": "integer",
          "description": "Credits used so far this period. Billable operations consume credits — how many depends on the operation and your plan — while failed calls cost nothing.",
          "example": 342
        },
        "remaining_credits": {
          "type": "integer",
          "nullable": true,
          "description": "Credits left in this period, or `null` when the plan has no ceiling.",
          "example": 658
        }
      },
      "required": [
        "plan",
        "period_start",
        "period_end",
        "resets_at",
        "included_credits",
        "used_credits",
        "remaining_credits"
      ],
      "x-january-upstream-schema": "CreditBalance"
    },
    "NutrientAmount": {
      "type": "object",
      "properties": {
        "value": {
          "type": "number",
          "example": 300
        },
        "unit": {
          "type": "string",
          "example": "g",
          "description": "Canonical across the API: g, mg, kcal, IU."
        }
      },
      "required": [
        "value",
        "unit"
      ],
      "x-january-upstream-schema": "NutrientAmount"
    },
    "NutritionFacts": {
      "type": "object",
      "properties": {
        "calories": {
          "$ref": "#/components/schemas/NutrientAmount"
        },
        "protein": {
          "$ref": "#/components/schemas/NutrientAmount"
        },
        "carbohydrates": {
          "$ref": "#/components/schemas/NutrientAmount"
        },
        "net_carbohydrates": {
          "$ref": "#/components/schemas/NutrientAmount"
        },
        "total_fat": {
          "$ref": "#/components/schemas/NutrientAmount"
        },
        "trans_fat": {
          "$ref": "#/components/schemas/NutrientAmount"
        },
        "saturated_fat": {
          "$ref": "#/components/schemas/NutrientAmount"
        },
        "fiber": {
          "$ref": "#/components/schemas/NutrientAmount"
        },
        "total_sugars": {
          "$ref": "#/components/schemas/NutrientAmount"
        },
        "added_sugars": {
          "$ref": "#/components/schemas/NutrientAmount"
        },
        "cholesterol": {
          "$ref": "#/components/schemas/NutrientAmount"
        },
        "calcium": {
          "$ref": "#/components/schemas/NutrientAmount"
        },
        "iron": {
          "$ref": "#/components/schemas/NutrientAmount"
        },
        "potassium": {
          "$ref": "#/components/schemas/NutrientAmount"
        },
        "sodium": {
          "$ref": "#/components/schemas/NutrientAmount"
        },
        "vitamin_d": {
          "$ref": "#/components/schemas/NutrientAmount"
        }
      },
      "x-january-upstream-schema": "Nutrients"
    },
    "ServingOption": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "nullable": true,
          "example": "68051535",
          "description": "Opaque serving id; may look numeric but is always a string."
        },
        "quantity": {
          "type": "number",
          "nullable": true,
          "example": 1
        },
        "unit": {
          "type": "string",
          "nullable": true,
          "example": "oz"
        },
        "scaling_factor": {
          "type": "number",
          "nullable": true,
          "description": "Multiplier applied to the food's nutrition values for this serving.",
          "example": 1
        },
        "weight_grams": {
          "type": "number",
          "nullable": true,
          "example": 28
        },
        "is_primary": {
          "type": "boolean",
          "nullable": true,
          "description": "Whether this is the default serving for the food.",
          "example": true
        }
      },
      "required": [
        "id",
        "quantity",
        "unit",
        "scaling_factor",
        "weight_grams",
        "is_primary"
      ],
      "x-january-upstream-schema": "FoodServing"
    },
    "FoodSearchItem": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "example": "101963552",
          "description": "Opaque id — pass it back verbatim to log, predict, or fetch this food."
        },
        "type": {
          "type": "string",
          "enum": [
            "generic",
            "branded",
            "recipe"
          ],
          "example": "branded",
          "description": "What kind of food this is: `generic` for a database staple (\"banana\"), `branded` for a packaged product, `recipe` for a multi-ingredient dish."
        },
        "name": {
          "type": "string",
          "nullable": true,
          "example": "Dipped Banana Bites",
          "description": "Null only when the database has no name for the row."
        },
        "brand_name": {
          "type": "string",
          "nullable": true,
          "example": "Banana",
          "description": "null for generic foods and recipes, which have no brand."
        },
        "nutrients": {
          "description": "Per-serving nutrition in the shared nutrient vocabulary. Keys are omitted when the database has no value.",
          "allOf": [
            {
              "$ref": "#/components/schemas/NutritionFacts"
            }
          ]
        },
        "glycemic_index": {
          "type": "number",
          "nullable": true,
          "description": "Glycemic index; null when the database has none for this food."
        },
        "glycemic_load": {
          "type": "number",
          "nullable": true,
          "description": "Glycemic load; null when the database has none for this food."
        },
        "image_url": {
          "type": "string",
          "nullable": true,
          "description": "URL of a picture of the food; null when the database has none."
        },
        "barcode": {
          "type": "string",
          "nullable": true,
          "example": "049000006346",
          "description": "The product's barcode; null for foods that have none. Named for the code rather than one of its encodings — the same field carries a UPC, an EAN or a GTIN. It is the database's normalized form, so it may differ from the digits you scanned in leading zeros: display it, do not string-compare it."
        },
        "servings": {
          "description": "Search and barcode results carry the default serving only; `GET /v1.2/foods/{food_id}` returns the complete list to choose from.",
          "type": "array",
          "items": {
            "$ref": "#/components/schemas/ServingOption"
          }
        }
      },
      "required": [
        "id",
        "type",
        "name",
        "brand_name",
        "nutrients",
        "glycemic_index",
        "glycemic_load",
        "image_url",
        "barcode",
        "servings"
      ],
      "x-january-upstream-schema": "Food"
    },
    "FoodSearchResults": {
      "type": "object",
      "properties": {
        "items": {
          "description": "Ranked matches, best first. Empty when nothing matches — not an error.",
          "type": "array",
          "items": {
            "$ref": "#/components/schemas/FoodSearchItem"
          }
        }
      },
      "required": [
        "items"
      ],
      "x-january-upstream-schema": "FoodSearchResponse"
    },
    "FoodSuggestion": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "example": "70376053",
          "description": "Opaque id — the same id `GET /v1.2/foods/{food_id}` takes."
        },
        "type": {
          "type": "string",
          "enum": [
            "generic",
            "branded"
          ],
          "example": "generic",
          "description": "What kind of food this is: `generic` for a database staple (\"banana\"), `branded` for a packaged product, `recipe` for a multi-ingredient dish."
        },
        "name": {
          "type": "string",
          "nullable": true,
          "example": "greek yogurt",
          "description": "Generic foods are lowercase; branded foods keep their product name. Null only when the index has no name for the row."
        },
        "brand_name": {
          "type": "string",
          "nullable": true,
          "example": "Chobani",
          "description": "null for generic (non-branded) foods."
        },
        "image_url": {
          "type": "string",
          "nullable": true,
          "description": "Thumbnail of the food; null when the database has none."
        },
        "nutrients": {
          "description": "Calories per default serving, in the shared nutrient vocabulary — the one nutrient a suggestion carries. Fetch the food for the full panel. Empty when the index has no value.",
          "allOf": [
            {
              "$ref": "#/components/schemas/NutritionFacts"
            }
          ]
        }
      },
      "required": [
        "id",
        "type",
        "name",
        "brand_name",
        "image_url",
        "nutrients"
      ],
      "x-january-upstream-schema": "FoodSuggestion"
    },
    "AutocompleteFoodsResponse": {
      "type": "object",
      "properties": {
        "items": {
          "description": "Ranked suggestions, generic foods before branded. Empty when nothing matches.",
          "type": "array",
          "items": {
            "$ref": "#/components/schemas/FoodSuggestion"
          }
        }
      },
      "required": [
        "items"
      ],
      "x-january-upstream-schema": "FoodSuggestionsResponse"
    },
    "SuggestFoodAlternativesBody": {
      "type": "object",
      "properties": {
        "diet_restrictions": {
          "type": "array",
          "description": "Allergens/ingredients to avoid. Omit it (or send []) if none apply.",
          "example": [
            "gluten",
            "peanuts"
          ],
          "items": {
            "$ref": "#/components/schemas/DietRestriction"
          }
        },
        "diet_preferences": {
          "type": "array",
          "description": "Dietary patterns to match. Omit it (or send []) if none apply.",
          "example": [
            "vegetarian"
          ],
          "items": {
            "$ref": "#/components/schemas/DietPreference"
          }
        }
      },
      "x-january-upstream-schema": "SuggestFoodAlternativesRequest"
    },
    "AlternativeServing": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "nullable": true,
          "example": "34237662",
          "description": "Null only when the producer sent a serving with no id."
        },
        "quantity": {
          "type": "number",
          "nullable": true,
          "example": 1,
          "description": "How much of `unit` this serving is; null when the producer reported none."
        },
        "unit": {
          "type": "string",
          "nullable": true,
          "example": "cup",
          "description": "Null only when the producer sent a serving with no unit."
        }
      },
      "required": [
        "id",
        "quantity",
        "unit"
      ],
      "x-january-upstream-schema": "ServingSummary"
    },
    "AlternativeFood": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "nullable": true,
          "example": "70379835",
          "description": "Catalog food id, or null when the producer matched none."
        },
        "name": {
          "type": "string",
          "nullable": true,
          "example": "Oatmeal",
          "description": "Null only when the producer sent a food with no name."
        },
        "brand_name": {
          "type": "string",
          "nullable": true,
          "example": null,
          "description": "Null for generic (non-branded) foods."
        },
        "nutrients": {
          "$ref": "#/components/schemas/NutritionFacts"
        },
        "servings": {
          "description": "Servings to read the nutrition against. Empty when the recommender returned none — the key itself is always present.",
          "type": "array",
          "items": {
            "$ref": "#/components/schemas/AlternativeServing"
          }
        }
      },
      "required": [
        "id",
        "name",
        "brand_name",
        "nutrients",
        "servings"
      ],
      "x-january-upstream-schema": "AlternativeFood"
    },
    "SuggestFoodAlternativesResponse": {
      "type": "object",
      "properties": {
        "alternatives": {
          "description": "Healthier alternatives matching the restrictions and preferences. An empty array is a valid result, not an error.",
          "type": "array",
          "items": {
            "$ref": "#/components/schemas/AlternativeFood"
          }
        }
      },
      "required": [
        "alternatives"
      ],
      "x-january-upstream-schema": "FoodAlternatives"
    },
    "Restaurant": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "restaurant"
          ],
          "example": "restaurant",
          "description": "Always `restaurant`: this endpoint answers places only. For dishes, use `GET /v1.2/menu-items`."
        },
        "id": {
          "type": "string",
          "example": "53fc3b8a-e6bf-404d-83c8-9f42124d1bee"
        },
        "name": {
          "type": "string",
          "nullable": true,
          "example": "McDonald's",
          "description": "Null only when the source has no name for the place."
        },
        "is_chain": {
          "type": "boolean",
          "nullable": true,
          "example": false,
          "description": "Whether this location belongs to a chain; null when unknown."
        },
        "distance_meters": {
          "type": "number",
          "nullable": true,
          "example": 124,
          "description": "Distance from (latitude, longitude) in meters; null when the source cannot place this result."
        },
        "city": {
          "type": "string",
          "nullable": true,
          "example": "San Francisco"
        },
        "address1": {
          "type": "string",
          "nullable": true,
          "example": "123 Main Street"
        },
        "address2": {
          "type": "string",
          "nullable": true,
          "example": "Suite 100",
          "description": "Second address line; null when there is none."
        }
      },
      "required": [
        "type",
        "id",
        "name",
        "is_chain",
        "distance_meters",
        "city",
        "address1",
        "address2"
      ],
      "x-january-upstream-schema": "Restaurant"
    },
    "SearchRestaurantsResponse": {
      "type": "object",
      "properties": {
        "items": {
          "description": "Matches ranked by proximity. Empty when nothing matches — not an error.",
          "type": "array",
          "items": {
            "$ref": "#/components/schemas/Restaurant"
          }
        }
      },
      "required": [
        "items"
      ],
      "x-january-upstream-schema": "RestaurantSearchResponse"
    },
    "RestaurantMenuItem": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "nullable": true,
          "example": "228990954",
          "description": "Food id of the dish — the same id `GET /v1.2/foods/{food_id}` and `POST /v1.2/food-logs` take. Null only when the menu source carries no id for the row."
        },
        "name": {
          "type": "string",
          "nullable": true,
          "example": "Cheeseburger",
          "description": "Null only when the menu source has no name for the dish."
        },
        "nutrients": {
          "description": "Per-serving nutrition in the shared nutrient vocabulary. Keys are omitted when the menu source has no value.",
          "allOf": [
            {
              "$ref": "#/components/schemas/NutritionFacts"
            }
          ]
        },
        "glycemic_index": {
          "type": "number",
          "nullable": true,
          "description": "Glycemic index; null when the source has none."
        },
        "glycemic_load": {
          "type": "number",
          "nullable": true,
          "description": "Glycemic load; null when the source has none."
        },
        "servings": {
          "description": "The serving the nutrition is given for. `GET /v1.2/foods/{food_id}` returns the complete list of servings.",
          "type": "array",
          "items": {
            "$ref": "#/components/schemas/ServingOption"
          }
        }
      },
      "required": [
        "id",
        "name",
        "nutrients",
        "glycemic_index",
        "glycemic_load",
        "servings"
      ],
      "x-january-upstream-schema": "RestaurantMenuItem"
    },
    "GetRestaurantMenuItemsResponse": {
      "type": "object",
      "properties": {
        "items": {
          "description": "The menu items, by name. Empty when the restaurant has no menu on record.",
          "type": "array",
          "items": {
            "$ref": "#/components/schemas/RestaurantMenuItem"
          }
        }
      },
      "required": [
        "items"
      ],
      "x-january-upstream-schema": "RestaurantMenuItemsResponse"
    },
    "RestaurantMenuSearchItem": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "menu_item"
          ],
          "example": "menu_item",
          "description": "Always `menu_item`: this endpoint answers dishes only. For the places themselves, use `GET /v1.2/restaurants`."
        },
        "id": {
          "$ref": "#/components/schemas/RestaurantMenuItemId"
        },
        "name": {
          "type": "string",
          "nullable": true,
          "example": "burger",
          "description": "Null only when the menu source has no name for the dish."
        },
        "restaurant_name": {
          "type": "string",
          "nullable": true,
          "example": "morning due cafe",
          "description": "Null when the menu source names no restaurant."
        },
        "is_chain": {
          "type": "boolean",
          "nullable": true,
          "example": false,
          "description": "Whether the restaurant belongs to a chain; null when unknown."
        },
        "nutrients": {
          "description": "Per-dish nutrition in the shared nutrient vocabulary. Keys are omitted when the menu source has no value.",
          "allOf": [
            {
              "$ref": "#/components/schemas/NutritionFacts"
            }
          ]
        },
        "glycemic_index": {
          "type": "number",
          "nullable": true,
          "description": "Glycemic index; null when the source has none."
        },
        "glycemic_load": {
          "type": "number",
          "nullable": true,
          "description": "Glycemic load; null when the source has none."
        },
        "image_url": {
          "type": "string",
          "nullable": true,
          "description": "URL of a picture of the dish; null when the source has none."
        },
        "distance_meters": {
          "type": "number",
          "nullable": true,
          "example": 124,
          "description": "Distance from (latitude, longitude) in meters."
        },
        "servings": {
          "type": "array",
          "items": {
            "$ref": "#/components/schemas/ServingOption"
          }
        }
      },
      "required": [
        "type",
        "id",
        "name",
        "restaurant_name",
        "is_chain",
        "nutrients",
        "glycemic_index",
        "glycemic_load",
        "image_url",
        "distance_meters",
        "servings"
      ],
      "x-january-upstream-schema": "MenuItem"
    },
    "SearchRestaurantMenuItemsResponse": {
      "type": "object",
      "properties": {
        "items": {
          "description": "Matching dishes ranked by proximity. Empty when nothing matches — not an error.",
          "type": "array",
          "items": {
            "$ref": "#/components/schemas/RestaurantMenuSearchItem"
          }
        }
      },
      "required": [
        "items"
      ],
      "x-january-upstream-schema": "MenuItemSearchResponse"
    },
    "ScanFoodPhotoBody": {
      "type": "object",
      "properties": {
        "image": {
          "type": "string",
          "description": "The food photo — the food itself or a packaged product's label — as an http(s) URL or a base64 data URI (data:image/jpeg;base64,…). Formats: JPG, PNG, WEBP, and non-animated GIF. Around 1,024 px on the shorter side is enough for reliable results (a recommendation, not a validation rule). A URL must be publicly fetchable server-side — hosts that block hotlinking or require a login cannot be read — and has no enforced size cap, though very large files slow the analysis and can time out. Base64 must be a complete data URI and fit the 5 MB request-body cap, so keep raw images under ~3.5 MB before encoding (base64 inflates by ~33%). Prefer the URL when the image is already hosted.",
          "example": "https://i.imgur.com/bTQIGxf.png"
        }
      },
      "required": [
        "image"
      ],
      "x-january-upstream-schema": "ImageFoodAnalysisRequest"
    },
    "DetectedServing": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "nullable": true,
          "example": "34237662",
          "description": "Null only when the producer sent a serving with no id."
        },
        "quantity": {
          "type": "number",
          "nullable": true,
          "example": 1,
          "description": "How much of `unit` this serving is; null when the producer reported none."
        },
        "unit": {
          "type": "string",
          "nullable": true,
          "example": "cup",
          "description": "Null only when the producer sent a serving with no unit."
        },
        "selected_quantity": {
          "type": "number",
          "nullable": true,
          "description": "Quantity parsed from the text ('2 cups' → 2); null on image analyses. Advisory — corrections reads the serving's own quantity.",
          "example": 2
        }
      },
      "required": [
        "id",
        "quantity",
        "unit",
        "selected_quantity"
      ],
      "x-january-upstream-schema": "DetectionServing"
    },
    "DetectedFood": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "nullable": true,
          "example": "70379835",
          "description": "Catalog food id, or null when the producer matched none."
        },
        "name": {
          "type": "string",
          "nullable": true,
          "example": "Oatmeal",
          "description": "Null only when the producer sent a food with no name."
        },
        "brand_name": {
          "type": "string",
          "nullable": true,
          "example": null,
          "description": "Null for generic (non-branded) foods."
        },
        "nutrients": {
          "$ref": "#/components/schemas/NutritionFacts"
        },
        "servings": {
          "description": "Never empty: every detection producer guarantees at least one serving.",
          "type": "array",
          "items": {
            "$ref": "#/components/schemas/DetectedServing"
          }
        }
      },
      "required": [
        "id",
        "name",
        "brand_name",
        "nutrients",
        "servings"
      ],
      "x-january-upstream-schema": "DetectedFood"
    },
    "FoodDetection": {
      "type": "object",
      "properties": {
        "confidence": {
          "$ref": "#/components/schemas/ConfidenceScore"
        },
        "food": {
          "$ref": "#/components/schemas/DetectedFood"
        }
      },
      "required": [
        "confidence",
        "food"
      ],
      "x-january-upstream-schema": "Detection"
    },
    "FoodScan": {
      "type": "object",
      "properties": {
        "meal_name": {
          "type": "string",
          "nullable": true,
          "example": "Breakfast Bowl",
          "description": "A name for the meal as a whole. Null on text analyses — the caller already has the words."
        },
        "total_nutrients": {
          "description": "Aggregated nutrition across all detections. Always present; individual keys are omitted when no producer had a value.",
          "allOf": [
            {
              "$ref": "#/components/schemas/NutritionFacts"
            }
          ]
        },
        "detections": {
          "description": "Detected foods. Always present — an empty array means nothing was recognized.",
          "type": "array",
          "items": {
            "$ref": "#/components/schemas/FoodDetection"
          }
        }
      },
      "required": [
        "meal_name",
        "total_nutrients",
        "detections"
      ],
      "x-january-upstream-schema": "FoodAnalysisResult"
    },
    "SearchFoodsByNaturalLanguageBody": {
      "type": "object",
      "properties": {
        "text": {
          "type": "string",
          "description": "Natural-language description of what was eaten; parsed into detected foods with quantities.",
          "example": "a bowl of oatmeal with honey and a banana",
          "maxLength": 512
        }
      },
      "required": [
        "text"
      ],
      "x-january-upstream-schema": "TextFoodAnalysisRequest"
    },
    "CorrectPhotoScanBody": {
      "type": "object",
      "properties": {
        "analysis": {
          "description": "The result from `POST /v1.2/food-analysis/image` or `/text`, sent back exactly as it was returned. Omitted zero-value nutrient keys are filled in automatically, and `total_nutrients` is recalculated rather than trusted — send it or leave it out, it makes no difference.",
          "allOf": [
            {
              "$ref": "#/components/schemas/FoodScan"
            }
          ]
        },
        "instruction": {
          "type": "string",
          "description": "Plain-English description of what to correct.",
          "example": "The oatmeal was steel-cut, about 2 cups, and there was no honey.",
          "maxLength": 1000
        }
      },
      "required": [
        "analysis",
        "instruction"
      ],
      "x-january-upstream-schema": "CorrectFoodAnalysisRequest"
    },
    "FoodLogInputFood": {
      "type": "object",
      "properties": {
        "food_id": {
          "$ref": "#/components/schemas/FoodId"
        },
        "serving_id": {
          "$ref": "#/components/schemas/ServingId"
        },
        "quantity": {
          "type": "number",
          "example": 1.4,
          "description": "How many of that serving were consumed.",
          "minimum": 0,
          "exclusiveMinimum": true,
          "maximum": 10000
        }
      },
      "required": [
        "food_id",
        "serving_id",
        "quantity"
      ],
      "x-january-upstream-schema": "FoodSelection"
    },
    "CreateFoodLogBody": {
      "type": "object",
      "properties": {
        "foods": {
          "maxItems": 100,
          "type": "array",
          "items": {
            "$ref": "#/components/schemas/FoodLogInputFood"
          }
        },
        "eaten_at": {
          "type": "string",
          "example": "2024-09-13T11:34:56Z",
          "format": "date-time",
          "description": "When the meal was eaten — any ISO-8601 offset; stored and returned in UTC with milliseconds. Omitted = now."
        },
        "name": {
          "type": "string",
          "example": "Breakfast",
          "maxLength": 256
        }
      },
      "required": [
        "foods"
      ],
      "x-january-upstream-schema": "CreateFoodLogRequest"
    },
    "ServingDetails": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "nullable": true,
          "example": "68051535",
          "description": "Null only when the upstream sent a serving with no id."
        },
        "quantity": {
          "type": "number",
          "nullable": true,
          "example": 1,
          "description": "How many units make up one of this serving, e.g. 1 for \"1 cup\". Null when the upstream reported none."
        },
        "unit": {
          "type": "string",
          "nullable": true,
          "example": "cup",
          "description": "Null only when the upstream sent a serving with no unit."
        },
        "weight_grams": {
          "type": "number",
          "nullable": true,
          "example": 245,
          "description": "Null when the upstream has no gram weight for this serving."
        }
      },
      "required": [
        "id",
        "quantity",
        "unit",
        "weight_grams"
      ],
      "x-january-upstream-schema": "ServingDetails"
    },
    "LoggedFood": {
      "type": "object",
      "properties": {
        "food_id": {
          "type": "string",
          "nullable": true,
          "example": "101963552",
          "description": "Food id from a search or food-analysis result. Null only when the upstream sent a food with no id."
        },
        "name": {
          "type": "string",
          "nullable": true,
          "example": "Greek Yogurt, Plain, Whole Milk",
          "description": "Null only when the upstream sent none."
        },
        "brand_name": {
          "type": "string",
          "nullable": true,
          "example": null,
          "description": "Null for generic (non-branded) foods."
        },
        "image_url": {
          "type": "string",
          "nullable": true,
          "example": null
        },
        "glycemic_index": {
          "type": "number",
          "nullable": true,
          "example": 11.3
        },
        "glycemic_load": {
          "type": "number",
          "nullable": true,
          "example": 1.4
        },
        "nutrients": {
          "description": "Scaled to the consumed quantity.",
          "allOf": [
            {
              "$ref": "#/components/schemas/NutritionFacts"
            }
          ]
        },
        "quantity": {
          "type": "number",
          "nullable": true,
          "example": 1.4,
          "description": "How many of the serving below were consumed. Null only when the upstream sent no consumed quantity."
        },
        "serving": {
          "description": "The serving definition the quantity refers to.",
          "allOf": [
            {
              "$ref": "#/components/schemas/ServingDetails"
            }
          ]
        }
      },
      "required": [
        "food_id",
        "name",
        "brand_name",
        "image_url",
        "glycemic_index",
        "glycemic_load",
        "nutrients",
        "quantity",
        "serving"
      ],
      "x-january-upstream-schema": "LoggedFood"
    },
    "FoodLog": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "nullable": true,
          "example": "78129823-8ba2-4183-b13b-71f0e963c606",
          "description": "Save this id to update or delete the log. Null only when the upstream sent a log with no id — such a log cannot be addressed."
        },
        "foods": {
          "type": "array",
          "items": {
            "$ref": "#/components/schemas/LoggedFood"
          }
        },
        "eaten_at": {
          "type": "string",
          "example": "2024-09-13T11:34:56.000Z",
          "format": "date-time",
          "description": "When the meal was eaten. UTC, with milliseconds."
        },
        "name": {
          "type": "string",
          "nullable": true,
          "example": "Breakfast",
          "description": "Null when no name was given."
        }
      },
      "required": [
        "id",
        "foods",
        "eaten_at",
        "name"
      ],
      "x-january-upstream-schema": "FoodLog"
    },
    "ListFoodLogsResponse": {
      "type": "object",
      "properties": {
        "items": {
          "description": "Logs in the range, ordered by timestamp. An empty list is a valid result.",
          "type": "array",
          "items": {
            "$ref": "#/components/schemas/FoodLog"
          }
        }
      },
      "required": [
        "items"
      ],
      "x-january-upstream-schema": "FoodLogList"
    },
    "UpdateFoodLogBody": {
      "type": "object",
      "properties": {
        "foods": {
          "maxItems": 100,
          "type": "array",
          "items": {
            "$ref": "#/components/schemas/FoodLogInputFood"
          }
        },
        "eaten_at": {
          "type": "string",
          "example": "2024-09-13T11:34:56Z",
          "format": "date-time",
          "description": "When the meal was eaten — any ISO-8601 offset; stored and returned in UTC with milliseconds. Omit to leave it unchanged."
        },
        "name": {
          "type": "string",
          "example": "Breakfast",
          "maxLength": 256
        }
      },
      "x-january-upstream-schema": "UpdateFoodLogRequest"
    },
    "Height": {
      "type": "object",
      "properties": {
        "value": {
          "type": "number",
          "example": 66
        },
        "unit": {
          "$ref": "#/components/schemas/HeightUnit"
        }
      },
      "required": [
        "value",
        "unit"
      ],
      "x-january-upstream-schema": "Height"
    },
    "Weight": {
      "type": "object",
      "properties": {
        "value": {
          "type": "number",
          "example": 150
        },
        "unit": {
          "$ref": "#/components/schemas/WeightUnit"
        }
      },
      "required": [
        "value",
        "unit"
      ],
      "x-january-upstream-schema": "Weight"
    },
    "GlucosePredictionProfile": {
      "type": "object",
      "properties": {
        "age": {
          "type": "number",
          "example": 42
        },
        "sex": {
          "$ref": "#/components/schemas/Sex"
        },
        "height": {
          "$ref": "#/components/schemas/Height"
        },
        "weight": {
          "$ref": "#/components/schemas/Weight"
        },
        "activity_level": {
          "$ref": "#/components/schemas/ActivityLevel"
        },
        "health_conditions": {
          "type": "array",
          "example": [
            "prediabetes"
          ],
          "description": "Omit it (or send []) if none apply. Type 1 diabetes is not supported by the prediction model.",
          "items": {
            "$ref": "#/components/schemas/MedicalCondition"
          }
        }
      },
      "required": [
        "age",
        "sex",
        "height",
        "weight"
      ],
      "x-january-upstream-schema": "GlucoseUserProfile"
    },
    "CgmReading": {
      "type": "object",
      "properties": {
        "timestamp": {
          "type": "string",
          "example": "2024-09-10T08:15:00Z",
          "format": "date-time",
          "description": "When the reading was taken. Must carry a timezone designator."
        },
        "value": {
          "type": "number",
          "example": 104,
          "description": "mg/dL. At most one reading per 15-minute window."
        }
      },
      "required": [
        "timestamp",
        "value"
      ],
      "x-january-upstream-schema": "CgmReading"
    },
    "ConsumedHistoricalFood": {
      "type": "object",
      "properties": {
        "timestamp": {
          "type": "string",
          "example": "2024-09-10T08:00:00Z",
          "format": "date-time",
          "description": "When the food was eaten. Must carry a timezone designator."
        },
        "food_id": {
          "type": "string",
          "example": "101963552",
          "description": "Food id from a search or food-analysis result."
        },
        "serving_id": {
          "type": "string",
          "example": "68051535",
          "description": "One of the food's serving ids."
        },
        "quantity": {
          "type": "number",
          "example": 1.4,
          "description": "How many of that serving were consumed.",
          "minimum": 0,
          "exclusiveMinimum": true,
          "maximum": 10000
        }
      },
      "required": [
        "timestamp",
        "food_id",
        "serving_id",
        "quantity"
      ],
      "x-january-upstream-schema": "ConsumedFood"
    },
    "PredictGlucoseBody": {
      "type": "object",
      "properties": {
        "user_profile": {
          "$ref": "#/components/schemas/GlucosePredictionProfile"
        },
        "timezone": {
          "type": "string",
          "example": "America/New_York",
          "description": "The IANA timezone the end user is in. The prediction depends on the meal's local time of day."
        },
        "foods": {
          "description": "The meal to predict the glucose response for.",
          "maxItems": 100,
          "type": "array",
          "items": {
            "$ref": "#/components/schemas/FoodLogInputFood"
          }
        },
        "start_time": {
          "type": "string",
          "example": "2024-09-13T11:34:56Z",
          "format": "date-time",
          "description": "When the meal is (or will be) eaten. Must carry a timezone designator."
        },
        "cgm_data": {
          "description": "Optional CGM history, to personalize the prediction to this end user. Send it together with `consumed_foods` covering the same period — **at least 5 complete days of paired history**, which is what the model needs to train on them. Fewer is refused with `invalid_request`. Omit both fields for a standard prediction, which needs no sensor and no history.",
          "type": "array",
          "items": {
            "$ref": "#/components/schemas/CgmReading"
          }
        },
        "consumed_foods": {
          "description": "The meals eaten during the CGM history; requires cgm_data.",
          "type": "array",
          "items": {
            "$ref": "#/components/schemas/ConsumedHistoricalFood"
          }
        }
      },
      "required": [
        "user_profile",
        "timezone",
        "foods",
        "start_time"
      ],
      "x-january-upstream-schema": "GlucosePredictionRequest"
    },
    "GlucosePredictionPoint": {
      "type": "object",
      "properties": {
        "minutes": {
          "type": "integer",
          "example": 30,
          "description": "Minutes after start_time."
        },
        "value": {
          "type": "number",
          "example": 140,
          "description": "Predicted glucose, mg/dL."
        }
      },
      "required": [
        "minutes",
        "value"
      ],
      "x-january-upstream-schema": "GlucosePredictionPoint"
    },
    "GlucoseChart": {
      "type": "object",
      "properties": {
        "min": {
          "type": "number",
          "nullable": true,
          "example": 70,
          "description": "Suggested Y-axis lower bound (mg/dL). A fixed target-range bound, not the minimum of the curve. Null when the model sent none."
        },
        "max": {
          "type": "number",
          "nullable": true,
          "example": 140,
          "description": "Suggested Y-axis upper bound (mg/dL): 180 with Type 2 diabetes in health_conditions, otherwise 140. Not the maximum of the curve. Null when the model sent none."
        }
      },
      "required": [
        "min",
        "max"
      ],
      "x-january-upstream-schema": "GlucoseChart"
    },
    "GlucosePrediction": {
      "type": "object",
      "properties": {
        "points": {
          "description": "The points of the predicted glucose curve, at 15-minute intervals starting at start_time.",
          "type": "array",
          "items": {
            "$ref": "#/components/schemas/GlucosePredictionPoint"
          }
        },
        "impact_score": {
          "$ref": "#/components/schemas/GlucoseImpact"
        },
        "chart": {
          "$ref": "#/components/schemas/GlucoseChart"
        }
      },
      "required": [
        "points",
        "impact_score",
        "chart"
      ],
      "x-january-upstream-schema": "GlucosePrediction"
    },
    "FoodId": {
      "type": "string",
      "format": "opaque",
      "pattern": "^[0-9]{1,16}$",
      "example": "133892962",
      "description": "Opaque food identifier; pass it back verbatim.",
      "x-january-derived-from": [
        "/paths/~1v1.2~1foods~1{food_id}/get/parameters/0/schema",
        "/paths/~1v1.2~1foods~1{food_id}~1alternatives/post/parameters/0/schema",
        "/components/schemas/FoodSelection/properties/food_id"
      ],
      "x-january-overlay-id": "NORMALIZATION-002"
    },
    "ServingId": {
      "type": "string",
      "format": "opaque",
      "pattern": "^[0-9]{1,16}$",
      "example": "142722827",
      "description": "Opaque serving identifier; pass it back verbatim.",
      "x-january-derived-from": [
        "/components/schemas/FoodSelection/properties/serving_id"
      ],
      "x-january-overlay-id": "NORMALIZATION-003"
    },
    "FoodLogId": {
      "type": "string",
      "format": "uuid",
      "example": "78129823-8ba2-4183-b13b-71f0e963c606",
      "description": "Stable food-log identifier.",
      "x-january-derived-from": [
        "/paths/~1v1.2~1food-logs~1{log_id}/get/parameters/1/schema",
        "/paths/~1v1.2~1food-logs~1{log_id}/patch/parameters/1/schema",
        "/paths/~1v1.2~1food-logs~1{log_id}/delete/parameters/1/schema"
      ],
      "x-january-overlay-id": "NORMALIZATION-004"
    },
    "PartnerUserId": {
      "type": "string",
      "format": "opaque",
      "minLength": 1,
      "description": "Stable partner-owned identifier of the end user for whom a request is made.",
      "x-january-derived-from": [
        "/components/parameters/JanuaryEndUserId/schema"
      ],
      "x-january-overlay-id": "NORMALIZATION-005"
    },
    "RestaurantMenuItemId": {
      "type": "string",
      "format": "opaque",
      "minLength": 1,
      "example": "228990954",
      "description": "Opaque restaurant menu-item identifier.",
      "x-january-derived-from": [
        "/components/schemas/MenuItem/properties/id"
      ],
      "x-january-overlay-id": "NORMALIZATION-006"
    },
    "Barcode": {
      "type": "string",
      "pattern": "^[0-9]{6,14}$",
      "example": "049000006346",
      "x-january-derived-from": [
        "/paths/~1v1.2~1foods~1barcode~1{barcode}/get/parameters/0/schema"
      ],
      "x-january-overlay-id": "NORMALIZATION-007"
    },
    "FoodCategory": {
      "enum": [
        "generic",
        "branded",
        "recipe"
      ],
      "type": "string",
      "x-january-derived-from": [
        "/paths/~1v1.2~1foods/get/parameters/1/schema"
      ],
      "x-january-overlay-id": "NORMALIZATION-008"
    },
    "AutocompleteFoodCategory": {
      "enum": [
        "generic",
        "branded"
      ],
      "type": "string",
      "x-january-derived-from": [
        "/paths/~1v1.2~1foods~1autocomplete/get/parameters/1/schema"
      ],
      "x-january-overlay-id": "NORMALIZATION-064"
    },
    "DietRestriction": {
      "type": "string",
      "enum": [
        "gluten",
        "lactose",
        "yeast",
        "tree_nuts",
        "peanuts",
        "dairy",
        "eggs",
        "sulfites",
        "soy",
        "wheat",
        "shellfish",
        "fish",
        "mushrooms",
        "sesame",
        "msg",
        "caffeine",
        "fodmaps"
      ],
      "x-january-derived-from": [
        "/components/schemas/SuggestFoodAlternativesRequest/properties/diet_restrictions/items"
      ],
      "x-january-overlay-id": "NORMALIZATION-009"
    },
    "DietPreference": {
      "type": "string",
      "enum": [
        "vegetarian",
        "vegan",
        "keto",
        "paleo",
        "pescatarian",
        "low_carbohydrate",
        "high_protein",
        "kosher",
        "halal"
      ],
      "x-january-derived-from": [
        "/components/schemas/SuggestFoodAlternativesRequest/properties/diet_preferences/items"
      ],
      "x-january-overlay-id": "NORMALIZATION-010"
    },
    "ConfidenceScore": {
      "type": "string",
      "enum": [
        "high",
        "medium",
        "low"
      ],
      "nullable": true,
      "example": "high",
      "description": "How sure the vision model is of this detection. Null on text analyses and on corrected results.",
      "x-january-derived-from": [
        "/components/schemas/Detection/properties/confidence"
      ],
      "x-january-overlay-id": "NORMALIZATION-011"
    },
    "Sex": {
      "type": "string",
      "enum": [
        "male",
        "female"
      ],
      "example": "female",
      "description": "Biological sex, as consumed by the prediction model.",
      "x-january-derived-from": [
        "/components/schemas/GlucoseUserProfile/properties/sex"
      ],
      "x-january-overlay-id": "NORMALIZATION-012"
    },
    "ActivityLevel": {
      "type": "string",
      "enum": [
        "sedentary",
        "lightly_active",
        "moderately_active",
        "very_active"
      ],
      "example": "moderately_active",
      "x-january-derived-from": [
        "/components/schemas/GlucoseUserProfile/properties/activity_level"
      ],
      "x-january-overlay-id": "NORMALIZATION-013"
    },
    "MedicalCondition": {
      "type": "string",
      "enum": [
        "type_2_diabetes",
        "prediabetes"
      ],
      "x-january-derived-from": [
        "/components/schemas/GlucoseUserProfile/properties/health_conditions/items"
      ],
      "x-january-overlay-id": "NORMALIZATION-014"
    },
    "GlucoseImpact": {
      "type": "string",
      "enum": [
        "low",
        "medium",
        "high"
      ],
      "nullable": true,
      "example": "low",
      "description": "The meal's overall glucose impact. Null when the model returned no grade, or one outside this list.",
      "x-january-derived-from": [
        "/components/schemas/GlucosePrediction/properties/impact_score"
      ],
      "x-january-overlay-id": "NORMALIZATION-015"
    },
    "HeightUnit": {
      "type": "string",
      "enum": [
        "in",
        "cm"
      ],
      "example": "in",
      "x-january-derived-from": [
        "/components/schemas/Height/properties/unit"
      ],
      "x-january-overlay-id": "NORMALIZATION-016"
    },
    "WeightUnit": {
      "type": "string",
      "enum": [
        "lb",
        "kg"
      ],
      "example": "lb",
      "x-january-derived-from": [
        "/components/schemas/Weight/properties/unit"
      ],
      "x-january-overlay-id": "NORMALIZATION-062"
    }
  },
  "operations": {
    "createClientToken": {
      "operationId": "createClientToken",
      "method": "POST",
      "path": "/v1.2/auth/client-tokens",
      "resource": null,
      "publicMethod": "createClientToken",
      "audience": "server",
      "parameterNames": {
        "January-End-User-ID": "endUserId",
        "x-end-user-id": "endUserId",
        "x-end-user-timezone": "endUserTimezone"
      },
      "parameters": [],
      "fields": [
        {
          "name": "end_user_id",
          "publicName": "end_user_id",
          "schema": {
            "type": "string",
            "description": "Your stable ID for the end user this token acts as. The token is bound to it; requests made with the token act only on this user.",
            "example": "acme-user-8271",
            "maxLength": 64
          },
          "required": true
        },
        {
          "name": "scopes",
          "publicName": "scopes",
          "schema": {
            "type": "array",
            "description": "What the token may do. **Required** — name only the scopes this token needs (least privilege), never the full set out of convenience. A read-only food-lookup screen asks for `[\"foods:read\"]`; a logging screen adds `food_logs:write`. Valid scopes: foods:read, food_analysis:write, food_logs:read, food_logs:write, glucose:read, restaurants:read.",
            "example": [
              "foods:read"
            ],
            "items": {
              "type": "string",
              "enum": [
                "foods:read",
                "food_analysis:write",
                "food_logs:read",
                "food_logs:write",
                "glucose:read",
                "restaurants:read"
              ]
            }
          },
          "required": true
        },
        {
          "name": "ttl_seconds",
          "publicName": "ttl_seconds",
          "schema": {
            "type": "integer",
            "description": "How long the token stays valid, in seconds. Between 300 and 7200; defaults to 1800.",
            "minimum": 300,
            "maximum": 7200,
            "default": 1800,
            "example": 1800
          },
          "required": false
        }
      ],
      "bodySchema": {
        "$ref": "#/components/schemas/CreateClientTokenBody"
      },
      "responses": {
        "201": {
          "$ref": "#/components/schemas/ClientToken"
        }
      },
      "responseType": "ClientToken",
      "retryNever": false,
      "retryAmbiguous": false,
      "photoPreparation": false
    },
    "revokeClientTokens": {
      "operationId": "revokeClientTokens",
      "method": "POST",
      "path": "/v1.2/auth/client-token-revocations",
      "resource": null,
      "publicMethod": "revokeClientTokens",
      "audience": "server",
      "parameterNames": {
        "January-End-User-ID": "endUserId",
        "x-end-user-id": "endUserId",
        "x-end-user-timezone": "endUserTimezone"
      },
      "parameters": [],
      "fields": [
        {
          "name": "end_user_id",
          "publicName": "end_user_id",
          "schema": {
            "type": "string",
            "description": "The end user whose outstanding client tokens should all be revoked.",
            "example": "acme-user-8271",
            "maxLength": 64
          },
          "required": true
        }
      ],
      "bodySchema": {
        "$ref": "#/components/schemas/RevokeClientTokensBody"
      },
      "responses": {
        "200": {
          "$ref": "#/components/schemas/ClientTokenRevocationResult"
        }
      },
      "responseType": "ClientTokenRevocationResult",
      "retryNever": true,
      "retryAmbiguous": true,
      "photoPreparation": false
    },
    "getCredits": {
      "operationId": "getCredits",
      "method": "GET",
      "path": "/v1.2/credits",
      "resource": null,
      "publicMethod": "getCredits",
      "audience": "server",
      "parameterNames": {
        "January-End-User-ID": "endUserId",
        "x-end-user-id": "endUserId",
        "x-end-user-timezone": "endUserTimezone"
      },
      "parameters": [],
      "fields": [],
      "bodySchema": null,
      "responses": {
        "200": {
          "$ref": "#/components/schemas/CreditBalance"
        }
      },
      "responseType": "CreditBalance",
      "retryNever": false,
      "retryAmbiguous": true,
      "photoPreparation": false
    },
    "searchFoods": {
      "operationId": "searchFoods",
      "method": "GET",
      "path": "/v1.2/foods",
      "resource": "foods",
      "publicMethod": "search",
      "audience": "shared",
      "parameterNames": {
        "January-End-User-ID": "endUserId",
        "x-end-user-id": "endUserId",
        "x-end-user-timezone": "endUserTimezone"
      },
      "parameters": [
        {
          "name": "query",
          "required": true,
          "in": "query",
          "description": "The food name to search for.",
          "schema": {
            "maxLength": 256,
            "example": "greek yogurt",
            "type": "string"
          },
          "publicName": "query"
        },
        {
          "name": "type",
          "required": false,
          "in": "query",
          "description": "Narrows results to one kind of food. Omitted, all three are searched and returned as one ranked list, so a partner who does not care which kind a match is does not have to ask three times.",
          "schema": {
            "$ref": "#/components/schemas/FoodCategory"
          },
          "publicName": "type_"
        },
        {
          "name": "limit",
          "required": false,
          "in": "query",
          "description": "Maximum number of results to return.",
          "schema": {
            "minimum": 1,
            "maximum": 40,
            "default": 10,
            "type": "integer"
          },
          "publicName": "limit"
        }
      ],
      "fields": [],
      "bodySchema": null,
      "responses": {
        "200": {
          "$ref": "#/components/schemas/FoodSearchResults"
        }
      },
      "responseType": "FoodSearchResults",
      "retryNever": false,
      "retryAmbiguous": true,
      "photoPreparation": false
    },
    "autocompleteFoods": {
      "operationId": "autocompleteFoods",
      "method": "GET",
      "path": "/v1.2/foods/autocomplete",
      "resource": "foods",
      "publicMethod": "autocomplete",
      "audience": "shared",
      "parameterNames": {
        "January-End-User-ID": "endUserId",
        "x-end-user-id": "endUserId",
        "x-end-user-timezone": "endUserTimezone"
      },
      "parameters": [
        {
          "name": "query",
          "required": true,
          "in": "query",
          "description": "The characters the user has typed so far. Fewer than 2 letters or digits yield no suggestions.",
          "schema": {
            "maxLength": 64,
            "example": "ban",
            "type": "string"
          },
          "publicName": "query"
        },
        {
          "name": "type",
          "required": false,
          "in": "query",
          "description": "Narrows suggestions to one kind of food. Omitted, generic and branded foods are suggested together, generic first.",
          "schema": {
            "$ref": "#/components/schemas/AutocompleteFoodCategory"
          },
          "publicName": "type_"
        },
        {
          "name": "limit",
          "required": false,
          "in": "query",
          "description": "Maximum number of suggestions to return.",
          "schema": {
            "minimum": 1,
            "maximum": 20,
            "default": 8,
            "type": "integer"
          },
          "publicName": "limit"
        }
      ],
      "fields": [],
      "bodySchema": null,
      "responses": {
        "200": {
          "$ref": "#/components/schemas/AutocompleteFoodsResponse"
        }
      },
      "responseType": "AutocompleteFoodsResponse",
      "retryNever": false,
      "retryAmbiguous": true,
      "photoPreparation": false
    },
    "suggestFoodAlternatives": {
      "operationId": "suggestFoodAlternatives",
      "method": "POST",
      "path": "/v1.2/foods/{food_id}/alternatives",
      "resource": "foods",
      "publicMethod": "suggestAlternatives",
      "audience": "shared",
      "parameterNames": {
        "January-End-User-ID": "endUserId",
        "x-end-user-id": "endUserId",
        "x-end-user-timezone": "endUserTimezone"
      },
      "parameters": [
        {
          "name": "food_id",
          "required": true,
          "in": "path",
          "description": "Food id from a search or food-analysis result. The example is brown rice.",
          "schema": {
            "$ref": "#/components/schemas/FoodId"
          },
          "publicName": "food_id"
        }
      ],
      "fields": [
        {
          "name": "diet_restrictions",
          "publicName": "diet_restrictions",
          "schema": {
            "type": "array",
            "description": "Allergens/ingredients to avoid. Omit it (or send []) if none apply.",
            "example": [
              "gluten",
              "peanuts"
            ],
            "items": {
              "$ref": "#/components/schemas/DietRestriction"
            }
          },
          "required": false
        },
        {
          "name": "diet_preferences",
          "publicName": "diet_preferences",
          "schema": {
            "type": "array",
            "description": "Dietary patterns to match. Omit it (or send []) if none apply.",
            "example": [
              "vegetarian"
            ],
            "items": {
              "$ref": "#/components/schemas/DietPreference"
            }
          },
          "required": false
        }
      ],
      "bodySchema": {
        "$ref": "#/components/schemas/SuggestFoodAlternativesBody"
      },
      "responses": {
        "200": {
          "$ref": "#/components/schemas/SuggestFoodAlternativesResponse"
        }
      },
      "responseType": "SuggestFoodAlternativesResponse",
      "retryNever": false,
      "retryAmbiguous": true,
      "photoPreparation": false
    },
    "lookupFoodByBarcode": {
      "operationId": "lookupFoodByBarcode",
      "method": "GET",
      "path": "/v1.2/foods/barcode/{barcode}",
      "resource": "foods",
      "publicMethod": "lookupBarcode",
      "audience": "shared",
      "parameterNames": {
        "January-End-User-ID": "endUserId",
        "x-end-user-id": "endUserId",
        "x-end-user-timezone": "endUserTimezone"
      },
      "parameters": [
        {
          "name": "barcode",
          "required": true,
          "in": "path",
          "description": "The numeric barcode: 6 to 14 digits (UPC-E, UPC-A, EAN-8, EAN-13 or GTIN-14). The example is a Coca-Cola can.",
          "schema": {
            "$ref": "#/components/schemas/Barcode"
          },
          "publicName": "barcode"
        }
      ],
      "fields": [],
      "bodySchema": null,
      "responses": {
        "200": {
          "$ref": "#/components/schemas/FoodSearchItem"
        }
      },
      "responseType": "FoodSearchItem",
      "retryNever": false,
      "retryAmbiguous": true,
      "photoPreparation": false
    },
    "getFood": {
      "operationId": "getFood",
      "method": "GET",
      "path": "/v1.2/foods/{food_id}",
      "resource": "foods",
      "publicMethod": "get",
      "audience": "shared",
      "parameterNames": {
        "January-End-User-ID": "endUserId",
        "x-end-user-id": "endUserId",
        "x-end-user-timezone": "endUserTimezone"
      },
      "parameters": [
        {
          "name": "food_id",
          "required": true,
          "in": "path",
          "description": "Food id from a search or food-analysis result.",
          "schema": {
            "$ref": "#/components/schemas/FoodId"
          },
          "publicName": "food_id"
        }
      ],
      "fields": [],
      "bodySchema": null,
      "responses": {
        "200": {
          "$ref": "#/components/schemas/FoodSearchItem"
        }
      },
      "responseType": "FoodSearchItem",
      "retryNever": false,
      "retryAmbiguous": true,
      "photoPreparation": false
    },
    "searchRestaurants": {
      "operationId": "searchRestaurants",
      "method": "GET",
      "path": "/v1.2/restaurants",
      "resource": "restaurants",
      "publicMethod": "search",
      "audience": "shared",
      "parameterNames": {
        "January-End-User-ID": "endUserId",
        "x-end-user-id": "endUserId",
        "x-end-user-timezone": "endUserTimezone"
      },
      "parameters": [
        {
          "name": "radius_meters",
          "required": false,
          "in": "query",
          "description": "Search radius in meters around (latitude, longitude), e.g. 5000 = 5 kilometers. A fractional value is accepted and rounded. Default 8000 meters (about 5 miles); maximum 50000 (about 31 miles).",
          "schema": {
            "minimum": 1,
            "maximum": 50000,
            "default": 8000,
            "example": 5000,
            "type": "number"
          },
          "publicName": "radius_meters"
        },
        {
          "name": "limit",
          "required": false,
          "in": "query",
          "description": "Maximum number of results to return.",
          "schema": {
            "minimum": 1,
            "maximum": 100,
            "default": 10,
            "type": "integer"
          },
          "publicName": "limit"
        },
        {
          "name": "query",
          "required": true,
          "in": "query",
          "description": "Restaurant name to search for.",
          "schema": {
            "maxLength": 256,
            "example": "sweetgreen",
            "type": "string"
          },
          "publicName": "query"
        },
        {
          "name": "latitude",
          "required": true,
          "in": "query",
          "description": "Latitude of the search location, e.g. 37.7749 (San Francisco).",
          "schema": {
            "minimum": -90,
            "maximum": 90,
            "example": 37.7749,
            "type": "number"
          },
          "publicName": "latitude"
        },
        {
          "name": "longitude",
          "required": true,
          "in": "query",
          "description": "Longitude of the search location, e.g. -122.4194 (San Francisco).",
          "schema": {
            "minimum": -180,
            "maximum": 180,
            "example": -122.4194,
            "type": "number"
          },
          "publicName": "longitude"
        }
      ],
      "fields": [],
      "bodySchema": null,
      "responses": {
        "200": {
          "$ref": "#/components/schemas/SearchRestaurantsResponse"
        }
      },
      "responseType": "SearchRestaurantsResponse",
      "retryNever": false,
      "retryAmbiguous": true,
      "photoPreparation": false
    },
    "getRestaurantMenuItems": {
      "operationId": "getRestaurantMenuItems",
      "method": "GET",
      "path": "/v1.2/restaurants/{restaurant_id}/menu-items",
      "resource": "restaurants",
      "publicMethod": "getMenuItems",
      "audience": "shared",
      "parameterNames": {
        "January-End-User-ID": "endUserId",
        "x-end-user-id": "endUserId",
        "x-end-user-timezone": "endUserTimezone"
      },
      "parameters": [
        {
          "name": "limit",
          "required": false,
          "in": "query",
          "description": "Maximum number of menu items to return. Raise it, or page with `offset`, for a long menu.",
          "schema": {
            "minimum": 1,
            "maximum": 500,
            "default": 100,
            "type": "integer"
          },
          "publicName": "limit"
        },
        {
          "name": "offset",
          "required": false,
          "in": "query",
          "description": "Number of menu items to skip, for paging: a page shorter than `limit` is the last one.",
          "schema": {
            "minimum": 0,
            "default": 0,
            "type": "integer"
          },
          "publicName": "offset"
        },
        {
          "name": "restaurant_id",
          "required": true,
          "in": "path",
          "description": "Restaurant id from a `GET /v1.2/restaurants` result.",
          "schema": {
            "type": "string",
            "pattern": "^[A-Za-z0-9_-]{1,64}$",
            "example": "53fc3b8a-e6bf-404d-83c8-9f42124d1bee"
          },
          "publicName": "restaurant_id"
        }
      ],
      "fields": [],
      "bodySchema": null,
      "responses": {
        "200": {
          "$ref": "#/components/schemas/GetRestaurantMenuItemsResponse"
        }
      },
      "responseType": "GetRestaurantMenuItemsResponse",
      "retryNever": false,
      "retryAmbiguous": true,
      "photoPreparation": false
    },
    "searchRestaurantMenuItems": {
      "operationId": "searchRestaurantMenuItems",
      "method": "GET",
      "path": "/v1.2/menu-items",
      "resource": "restaurants",
      "publicMethod": "searchMenuItems",
      "audience": "shared",
      "parameterNames": {
        "January-End-User-ID": "endUserId",
        "x-end-user-id": "endUserId",
        "x-end-user-timezone": "endUserTimezone"
      },
      "parameters": [
        {
          "name": "radius_meters",
          "required": false,
          "in": "query",
          "description": "Search radius in meters around (latitude, longitude), e.g. 5000 = 5 kilometers. A fractional value is accepted and rounded. Default 8000 meters (about 5 miles); maximum 50000 (about 31 miles).",
          "schema": {
            "minimum": 1,
            "maximum": 50000,
            "default": 8000,
            "example": 5000,
            "type": "number"
          },
          "publicName": "radius_meters"
        },
        {
          "name": "limit",
          "required": false,
          "in": "query",
          "description": "Maximum number of results to return.",
          "schema": {
            "minimum": 1,
            "maximum": 100,
            "default": 10,
            "type": "integer"
          },
          "publicName": "limit"
        },
        {
          "name": "query",
          "required": true,
          "in": "query",
          "description": "Dish or restaurant name to search for.",
          "schema": {
            "maxLength": 256,
            "example": "burger",
            "type": "string"
          },
          "publicName": "query"
        },
        {
          "name": "latitude",
          "required": true,
          "in": "query",
          "description": "Latitude of the search location, e.g. 37.7749 (San Francisco).",
          "schema": {
            "minimum": -90,
            "maximum": 90,
            "example": 37.7749,
            "type": "number"
          },
          "publicName": "latitude"
        },
        {
          "name": "longitude",
          "required": true,
          "in": "query",
          "description": "Longitude of the search location, e.g. -122.4194 (San Francisco).",
          "schema": {
            "minimum": -180,
            "maximum": 180,
            "example": -122.4194,
            "type": "number"
          },
          "publicName": "longitude"
        }
      ],
      "fields": [],
      "bodySchema": null,
      "responses": {
        "200": {
          "$ref": "#/components/schemas/SearchRestaurantMenuItemsResponse"
        }
      },
      "responseType": "SearchRestaurantMenuItemsResponse",
      "retryNever": false,
      "retryAmbiguous": true,
      "photoPreparation": false
    },
    "scanFoodPhoto": {
      "operationId": "scanFoodPhoto",
      "method": "POST",
      "path": "/v1.2/food-analysis/image",
      "resource": "foodAnalysis",
      "publicMethod": "analyzePhoto",
      "audience": "shared",
      "parameterNames": {
        "January-End-User-ID": "endUserId",
        "x-end-user-id": "endUserId",
        "x-end-user-timezone": "endUserTimezone"
      },
      "parameters": [],
      "fields": [
        {
          "name": "image",
          "publicName": "image",
          "schema": {
            "type": "string",
            "description": "The food photo — the food itself or a packaged product's label — as an http(s) URL or a base64 data URI (data:image/jpeg;base64,…). Formats: JPG, PNG, WEBP, and non-animated GIF. Around 1,024 px on the shorter side is enough for reliable results (a recommendation, not a validation rule). A URL must be publicly fetchable server-side — hosts that block hotlinking or require a login cannot be read — and has no enforced size cap, though very large files slow the analysis and can time out. Base64 must be a complete data URI and fit the 5 MB request-body cap, so keep raw images under ~3.5 MB before encoding (base64 inflates by ~33%). Prefer the URL when the image is already hosted.",
            "example": "https://i.imgur.com/bTQIGxf.png"
          },
          "required": true
        }
      ],
      "bodySchema": {
        "$ref": "#/components/schemas/ScanFoodPhotoBody"
      },
      "responses": {
        "200": {
          "$ref": "#/components/schemas/FoodScan"
        }
      },
      "responseType": "FoodScan",
      "retryNever": false,
      "retryAmbiguous": true,
      "photoPreparation": true
    },
    "searchFoodsByNaturalLanguage": {
      "operationId": "searchFoodsByNaturalLanguage",
      "method": "POST",
      "path": "/v1.2/food-analysis/text",
      "resource": "foodAnalysis",
      "publicMethod": "analyzeDescription",
      "bodyPropertyNames": {
        "text": "query"
      },
      "audience": "shared",
      "parameterNames": {
        "January-End-User-ID": "endUserId",
        "x-end-user-id": "endUserId",
        "x-end-user-timezone": "endUserTimezone"
      },
      "parameters": [],
      "fields": [
        {
          "name": "text",
          "publicName": "query",
          "schema": {
            "type": "string",
            "description": "Natural-language description of what was eaten; parsed into detected foods with quantities.",
            "example": "a bowl of oatmeal with honey and a banana",
            "maxLength": 512
          },
          "required": true
        }
      ],
      "bodySchema": {
        "$ref": "#/components/schemas/SearchFoodsByNaturalLanguageBody"
      },
      "responses": {
        "200": {
          "$ref": "#/components/schemas/FoodScan"
        }
      },
      "responseType": "FoodScan",
      "retryNever": false,
      "retryAmbiguous": true,
      "photoPreparation": false
    },
    "correctPhotoScan": {
      "operationId": "correctPhotoScan",
      "method": "POST",
      "path": "/v1.2/food-analysis/corrections",
      "resource": "foodAnalysis",
      "publicMethod": "correct",
      "audience": "shared",
      "parameterNames": {
        "January-End-User-ID": "endUserId",
        "x-end-user-id": "endUserId",
        "x-end-user-timezone": "endUserTimezone"
      },
      "parameters": [],
      "fields": [
        {
          "name": "analysis",
          "publicName": "analysis",
          "schema": {
            "description": "The result from `POST /v1.2/food-analysis/image` or `/text`, sent back exactly as it was returned. Omitted zero-value nutrient keys are filled in automatically, and `total_nutrients` is recalculated rather than trusted — send it or leave it out, it makes no difference.",
            "allOf": [
              {
                "$ref": "#/components/schemas/FoodScan"
              }
            ]
          },
          "required": true
        },
        {
          "name": "instruction",
          "publicName": "instruction",
          "schema": {
            "type": "string",
            "description": "Plain-English description of what to correct.",
            "example": "The oatmeal was steel-cut, about 2 cups, and there was no honey.",
            "maxLength": 1000
          },
          "required": true
        }
      ],
      "bodySchema": {
        "$ref": "#/components/schemas/CorrectPhotoScanBody"
      },
      "responses": {
        "200": {
          "$ref": "#/components/schemas/FoodScan"
        }
      },
      "responseType": "FoodScan",
      "retryNever": false,
      "retryAmbiguous": true,
      "photoPreparation": false
    },
    "createFoodLog": {
      "operationId": "createFoodLog",
      "method": "POST",
      "path": "/v1.2/food-logs",
      "resource": "foodLogs",
      "publicMethod": "create",
      "audience": "shared",
      "parameterNames": {
        "January-End-User-ID": "endUserId",
        "x-end-user-id": "endUserId",
        "x-end-user-timezone": "endUserTimezone"
      },
      "parameters": [
        {
          "name": "January-End-User-ID",
          "in": "header",
          "description": "Your stable ID for the end user whose food logs this request reads or writes. Opaque to January — use the same ID your system already uses for them.\n\n| Credential | Header | Result |\n| --- | --- | --- |\n| API key (`sk-…`) | absent | `400 end_user_id_required` |\n| API key (`sk-…`) | present | the request acts on that end user |\n| Client token (`ct-…`) | absent | filled in from the token |\n| Client token (`ct-…`) | the end user the token is bound to | accepted |\n| Client token (`ct-…`) | any other end user | `403 end_user_id_mismatch` |",
          "required": false,
          "schema": {
            "$ref": "#/components/schemas/PartnerUserId"
          },
          "publicName": "end_user_id"
        }
      ],
      "fields": [
        {
          "name": "foods",
          "publicName": "foods",
          "schema": {
            "maxItems": 100,
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/FoodLogInputFood"
            }
          },
          "required": true
        },
        {
          "name": "eaten_at",
          "publicName": "eaten_at",
          "schema": {
            "type": "string",
            "example": "2024-09-13T11:34:56Z",
            "format": "date-time",
            "description": "When the meal was eaten — any ISO-8601 offset; stored and returned in UTC with milliseconds. Omitted = now."
          },
          "required": false
        },
        {
          "name": "name",
          "publicName": "name",
          "schema": {
            "type": "string",
            "example": "Breakfast",
            "maxLength": 256
          },
          "required": false
        }
      ],
      "bodySchema": {
        "$ref": "#/components/schemas/CreateFoodLogBody"
      },
      "responses": {
        "201": {
          "$ref": "#/components/schemas/FoodLog"
        }
      },
      "responseType": "FoodLog",
      "retryNever": false,
      "retryAmbiguous": false,
      "photoPreparation": false
    },
    "listFoodLogs": {
      "operationId": "listFoodLogs",
      "method": "GET",
      "path": "/v1.2/food-logs",
      "resource": "foodLogs",
      "publicMethod": "list",
      "audience": "shared",
      "parameterNames": {
        "January-End-User-ID": "endUserId",
        "x-end-user-id": "endUserId",
        "x-end-user-timezone": "endUserTimezone"
      },
      "parameters": [
        {
          "name": "January-End-User-ID",
          "in": "header",
          "description": "Your stable ID for the end user whose food logs this request reads or writes. Opaque to January — use the same ID your system already uses for them.\n\n| Credential | Header | Result |\n| --- | --- | --- |\n| API key (`sk-…`) | absent | `400 end_user_id_required` |\n| API key (`sk-…`) | present | the request acts on that end user |\n| Client token (`ct-…`) | absent | filled in from the token |\n| Client token (`ct-…`) | the end user the token is bound to | accepted |\n| Client token (`ct-…`) | any other end user | `403 end_user_id_mismatch` |",
          "required": false,
          "schema": {
            "$ref": "#/components/schemas/PartnerUserId"
          },
          "publicName": "end_user_id"
        },
        {
          "name": "start_date",
          "required": true,
          "in": "query",
          "description": "First local calendar date in `timezone`, inclusive.",
          "schema": {
            "format": "date",
            "example": "2024-09-01",
            "type": "string"
          },
          "publicName": "start_date"
        },
        {
          "name": "end_date",
          "required": true,
          "in": "query",
          "description": "Last local calendar date in `timezone`, inclusive. May equal start_date for a single day. The inclusive range may not exceed 60 calendar days.",
          "schema": {
            "format": "date",
            "example": "2024-09-08",
            "type": "string"
          },
          "publicName": "end_date"
        },
        {
          "name": "timezone",
          "required": true,
          "in": "query",
          "description": "IANA timezone that defines the local calendar days this range covers — required, so the upstream groups by the same days the caller means.",
          "schema": {
            "example": "America/Los_Angeles",
            "type": "string"
          },
          "publicName": "timezone"
        }
      ],
      "fields": [],
      "bodySchema": null,
      "responses": {
        "200": {
          "$ref": "#/components/schemas/ListFoodLogsResponse"
        }
      },
      "responseType": "ListFoodLogsResponse",
      "retryNever": false,
      "retryAmbiguous": true,
      "photoPreparation": false
    },
    "getFoodLog": {
      "operationId": "getFoodLog",
      "method": "GET",
      "path": "/v1.2/food-logs/{log_id}",
      "resource": "foodLogs",
      "publicMethod": "get",
      "audience": "shared",
      "parameterNames": {
        "January-End-User-ID": "endUserId",
        "x-end-user-id": "endUserId",
        "x-end-user-timezone": "endUserTimezone"
      },
      "parameters": [
        {
          "name": "January-End-User-ID",
          "in": "header",
          "description": "Your stable ID for the end user whose food logs this request reads or writes. Opaque to January — use the same ID your system already uses for them.\n\n| Credential | Header | Result |\n| --- | --- | --- |\n| API key (`sk-…`) | absent | `400 end_user_id_required` |\n| API key (`sk-…`) | present | the request acts on that end user |\n| Client token (`ct-…`) | absent | filled in from the token |\n| Client token (`ct-…`) | the end user the token is bound to | accepted |\n| Client token (`ct-…`) | any other end user | `403 end_user_id_mismatch` |",
          "required": false,
          "schema": {
            "$ref": "#/components/schemas/PartnerUserId"
          },
          "publicName": "end_user_id"
        },
        {
          "name": "log_id",
          "required": true,
          "in": "path",
          "description": "The log id returned when the log was created.",
          "schema": {
            "$ref": "#/components/schemas/FoodLogId"
          },
          "publicName": "log_id"
        }
      ],
      "fields": [],
      "bodySchema": null,
      "responses": {
        "200": {
          "$ref": "#/components/schemas/FoodLog"
        }
      },
      "responseType": "FoodLog",
      "retryNever": false,
      "retryAmbiguous": true,
      "photoPreparation": false
    },
    "updateFoodLog": {
      "operationId": "updateFoodLog",
      "method": "PATCH",
      "path": "/v1.2/food-logs/{log_id}",
      "resource": "foodLogs",
      "publicMethod": "update",
      "audience": "shared",
      "parameterNames": {
        "January-End-User-ID": "endUserId",
        "x-end-user-id": "endUserId",
        "x-end-user-timezone": "endUserTimezone"
      },
      "parameters": [
        {
          "name": "January-End-User-ID",
          "in": "header",
          "description": "Your stable ID for the end user whose food logs this request reads or writes. Opaque to January — use the same ID your system already uses for them.\n\n| Credential | Header | Result |\n| --- | --- | --- |\n| API key (`sk-…`) | absent | `400 end_user_id_required` |\n| API key (`sk-…`) | present | the request acts on that end user |\n| Client token (`ct-…`) | absent | filled in from the token |\n| Client token (`ct-…`) | the end user the token is bound to | accepted |\n| Client token (`ct-…`) | any other end user | `403 end_user_id_mismatch` |",
          "required": false,
          "schema": {
            "$ref": "#/components/schemas/PartnerUserId"
          },
          "publicName": "end_user_id"
        },
        {
          "name": "log_id",
          "required": true,
          "in": "path",
          "description": "The log id returned when the log was created.",
          "schema": {
            "$ref": "#/components/schemas/FoodLogId"
          },
          "publicName": "log_id"
        }
      ],
      "fields": [
        {
          "name": "foods",
          "publicName": "foods",
          "schema": {
            "maxItems": 100,
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/FoodLogInputFood"
            }
          },
          "required": false
        },
        {
          "name": "eaten_at",
          "publicName": "eaten_at",
          "schema": {
            "type": "string",
            "example": "2024-09-13T11:34:56Z",
            "format": "date-time",
            "description": "When the meal was eaten — any ISO-8601 offset; stored and returned in UTC with milliseconds. Omit to leave it unchanged."
          },
          "required": false
        },
        {
          "name": "name",
          "publicName": "name",
          "schema": {
            "type": "string",
            "example": "Breakfast",
            "maxLength": 256
          },
          "required": false
        }
      ],
      "bodySchema": {
        "$ref": "#/components/schemas/UpdateFoodLogBody"
      },
      "responses": {
        "200": {
          "$ref": "#/components/schemas/FoodLog"
        }
      },
      "responseType": "FoodLog",
      "retryNever": false,
      "retryAmbiguous": true,
      "photoPreparation": false
    },
    "deleteFoodLog": {
      "operationId": "deleteFoodLog",
      "method": "DELETE",
      "path": "/v1.2/food-logs/{log_id}",
      "resource": "foodLogs",
      "publicMethod": "delete",
      "audience": "shared",
      "parameterNames": {
        "January-End-User-ID": "endUserId",
        "x-end-user-id": "endUserId",
        "x-end-user-timezone": "endUserTimezone"
      },
      "parameters": [
        {
          "name": "January-End-User-ID",
          "in": "header",
          "description": "Your stable ID for the end user whose food logs this request reads or writes. Opaque to January — use the same ID your system already uses for them.\n\n| Credential | Header | Result |\n| --- | --- | --- |\n| API key (`sk-…`) | absent | `400 end_user_id_required` |\n| API key (`sk-…`) | present | the request acts on that end user |\n| Client token (`ct-…`) | absent | filled in from the token |\n| Client token (`ct-…`) | the end user the token is bound to | accepted |\n| Client token (`ct-…`) | any other end user | `403 end_user_id_mismatch` |",
          "required": false,
          "schema": {
            "$ref": "#/components/schemas/PartnerUserId"
          },
          "publicName": "end_user_id"
        },
        {
          "name": "log_id",
          "required": true,
          "in": "path",
          "description": "The log id returned when the log was created.",
          "schema": {
            "$ref": "#/components/schemas/FoodLogId"
          },
          "publicName": "log_id"
        }
      ],
      "fields": [],
      "bodySchema": null,
      "responses": {
        "204": null
      },
      "responseType": "ResponseMetadata",
      "retryNever": false,
      "retryAmbiguous": true,
      "photoPreparation": false
    },
    "predictGlucose": {
      "operationId": "predictGlucose",
      "method": "POST",
      "path": "/v1.2/glucose/predictions",
      "resource": "glucose",
      "publicMethod": "predict",
      "audience": "shared",
      "parameterNames": {
        "January-End-User-ID": "endUserId",
        "x-end-user-id": "endUserId",
        "x-end-user-timezone": "endUserTimezone"
      },
      "parameters": [],
      "fields": [
        {
          "name": "user_profile",
          "publicName": "user_profile",
          "schema": {
            "$ref": "#/components/schemas/GlucosePredictionProfile"
          },
          "required": true
        },
        {
          "name": "timezone",
          "publicName": "timezone",
          "schema": {
            "type": "string",
            "example": "America/New_York",
            "description": "The IANA timezone the end user is in. The prediction depends on the meal's local time of day."
          },
          "required": true
        },
        {
          "name": "foods",
          "publicName": "foods",
          "schema": {
            "description": "The meal to predict the glucose response for.",
            "maxItems": 100,
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/FoodLogInputFood"
            }
          },
          "required": true
        },
        {
          "name": "start_time",
          "publicName": "start_time",
          "schema": {
            "type": "string",
            "example": "2024-09-13T11:34:56Z",
            "format": "date-time",
            "description": "When the meal is (or will be) eaten. Must carry a timezone designator."
          },
          "required": true
        },
        {
          "name": "cgm_data",
          "publicName": "cgm_data",
          "schema": {
            "description": "Optional CGM history, to personalize the prediction to this end user. Send it together with `consumed_foods` covering the same period — **at least 5 complete days of paired history**, which is what the model needs to train on them. Fewer is refused with `invalid_request`. Omit both fields for a standard prediction, which needs no sensor and no history.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CgmReading"
            }
          },
          "required": false
        },
        {
          "name": "consumed_foods",
          "publicName": "consumed_foods",
          "schema": {
            "description": "The meals eaten during the CGM history; requires cgm_data.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ConsumedHistoricalFood"
            }
          },
          "required": false
        }
      ],
      "bodySchema": {
        "$ref": "#/components/schemas/PredictGlucoseBody"
      },
      "responses": {
        "200": {
          "$ref": "#/components/schemas/GlucosePrediction"
        }
      },
      "responseType": "GlucosePrediction",
      "retryNever": false,
      "retryAmbiguous": true,
      "photoPreparation": false
    }
  },
  "pythonPolicy": {
    "automaticRetries": 2,
    "retryAfterSecondsPerCall": 60,
    "retryNever": [
      "revokeClientTokens"
    ],
    "retryAmbiguousNever": [
      "createClientToken",
      "createFoodLog"
    ]
  }
}
